# MEXC先物(Contract) APIキー。実ファイルは .env(gitignore)に置く。チャットに貼らない。
# MEXCのAPI管理で「先物(Futures/Contract)取引」権限を有効にしたキーを使う。
MEXCF_API_KEY=
MEXCF_API_SECRET=
# 実弾発注ガード。key_test.py --order のときだけ 1 にする。
MEXCF_LIVE=0

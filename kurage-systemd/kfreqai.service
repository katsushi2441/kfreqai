[Unit]
Description=Kurage Freq AI Trade (freqtrade, dry-run既定)
After=network-online.target

[Service]
Type=simple
WorkingDirectory=/home/kojima/work/kfreqai
ExecStart=/usr/bin/docker compose up
ExecStop=/usr/bin/docker compose down
Restart=always
RestartSec=10

[Install]
WantedBy=default.target
